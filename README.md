# IoT MOPS — Лабораторная работа

**Разработка простого IoT сервиса с событийной архитектурой**

Многоуровневая IoT-система для приёма, обработки и анализа данных от устройств с полным стеком наблюдаемости.

---

## 📋 Содержание

- [Архитектура](#архитектура)
- [Технологии](#технологии)
- [Сервисы](#сервисы)
- [Быстрый старт](#быстрый-старт)
- [API](#api)
- [Rule Engine](#rule-engine)
- [Метрики](#метрики)
- [Наблюдаемость](#наблюдаемость)
- [Веб-интерфейсы](#веб-интерфейсы)

---

## 🏗️ Архитектура

```
┌─────────────────┐     HTTP POST      ┌──────────────────┐
│  Data Simulator │ ─────────────────► │   IoT Controller │
│  (генератор)    │                    │   (порт 8080)    │
└─────────────────┘                    └────────┬─────────┘
                                                │
                                    ┌───────────┼───────────┐
                                    ▼           ▼           ▼
                              ┌─────────┐ ┌──────────┐ ┌──────────┐
                              │ MongoDB │ │ RabbitMQ │ │Prometheus│
                              │(хранение)│ │ (очередь)│ │(метрики) │
                              └─────────┘ └────┬─────┘ └──────────┘
                                               │
                                               ▼
                                    ┌──────────────────┐
                                    │   Rule Engine    │
                                    │   (порт 8081)    │
                                    └────────┬─────────┘
                                             │
                                    ┌────────┼────────┐
                                    ▼        ▼        ▼
                              ┌─────────┐ ┌───────┐ ┌────────┐
                              │ MongoDB │ │Grafana│ │ Kibana │
                              │(алерты) │ │(графики)│ │(логи)  │
                              └─────────┘ └───────┘ └────────┘
```

### Поток данных:

1. **Data Simulator** генерирует IoT-сообщения и отправляет их по HTTP
2. **IoT Controller** принимает сообщения, валидирует, сохраняет в MongoDB и публикует в RabbitMQ
3. **Rule Engine** читает сообщения из очереди, применяет правила и создаёт алерты
4. **Prometheus** собирает метрики с обоих сервисов
5. **Filebeat** собирает логи контейнеров и отправляет в Elasticsearch
6. **Grafana** визуализирует метрики, **Kibana** — логи

---

## 🛠️ Технологии

| Категория | Технология | Версия | Назначение |
|-----------|------------|--------|------------|
| **Язык** | Java | 17 | Основной язык |
| **Фреймворк** | Spring Boot | 3.3.7 | Микросервисы |
| **Сборка** | Maven | 3.x | Управление зависимостями |
| **БД** | MongoDB | 7 | Хранение сообщений и алертов |
| **Очередь** | RabbitMQ | 3.13 | Асинхронная обработка |
| **Метрики** | Prometheus | 2.52 | Сбор метрик |
| **Дашборды** | Grafana | 10.4 | Визуализация метрик |
| **Логи** | Elasticsearch | 8.12 | Хранение логов |
| **Логи UI** | Kibana | 8.12 | Просмотр логов |
| **Сбор логов** | Filebeat | 8.12 | Отправка логов в ELK |
| **БД UI** | Mongo Express | 1.0 | Веб-интерфейс MongoDB |
| **Контейнеризация** | Docker Compose | - | Оркестрация |

---

## 📦 Сервисы

### 1. IoT Controller (`iot-controller`)

**Назначение:** HTTP-вход для IoT-устройств, валидация, персистенция, публикация событий.

**Функции:**
- Приём HTTP POST запросов на `/api/ingest`
- Валидация входящих данных (deviceId, ts, payload)
- Сохранение в MongoDB коллекцию `iotMessage`
- Публикация в RabbitMQ exchange `iot.events` с routing key `device.{deviceId}`
- Генерация `correlationId` для трассировки
- Экспорт кастомных метрик в Prometheus

**Порт:** 8080

### 2. Rule Engine (`rule-engine`)

**Назначение:** Обработка событий и применение бизнес-правил.

**Функции:**
- Подписка на очередь `iot.events.queue`
- Применение мгновенных правил (instant rules)
- Применение оконных правил (window rules)
- Сохранение алертов в MongoDB коллекцию `alert`
- Экспорт метрик об алертах

**Порт:** 8081

### 3. Data Simulator (`data-simulator`)

**Назначение:** Генерация тестового трафика.

**Параметры:**
| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `SIMULATOR_DEVICES` | 5 | Количество устройств |
| `SIMULATOR_RATE` | 1 | Сообщений/сек на устройство |
| `SIMULATOR_DURATION_SECONDS` | 120 | Длительность работы |
| `SIMULATOR_TARGET_URL` | http://iot-controller:8080/api/ingest | URL назначения |

---

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose
- JDK 17 (для локальной сборки)
- Maven (для локальной сборки)

### Запуск

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd IoT-mops

# 2. Собрать проект
./mvnw clean package -DskipTests

# 3. Запустить все сервисы
docker-compose up --build -d

# 4. Проверить статус
docker-compose ps
```

### Остановка

```bash
docker-compose down
```

---

## 📡 API

### POST `/api/ingest`

Принять сообщение от IoT-устройства.

**Request:**
```json
{
  "deviceId": "device-1",
  "ts": "2026-01-07T12:00:00Z",
  "payload": {
    "a": 6.5,
    "b": 42
  }
}
```

**Response:** `202 Accepted`

**Пример curl:**
```bash
curl -X POST http://localhost:8080/api/ingest \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"sensor-01","ts":"2026-01-07T12:00:00Z","payload":{"a":7,"b":100}}'
```

### GET `/actuator/health`

Проверка состояния сервиса.

### GET `/actuator/prometheus`

Метрики в формате Prometheus.

---

## ⚙️ Rule Engine

### Мгновенное правило (Instant Rule)

**Условие:** `payload.a > 5`

**Поведение:** Если значение `a` в payload превышает 5, немедленно создаётся алерт.

**Пример:** Сообщение с `{"a": 7}` → алерт `instant-a-gt-5`

### Оконное правило (Window Rule)

**Условие:** Последние N=10 сообщений от одного устройства за не более 60 секунд, все с `payload.a > 5`

**Поведение:** 
- Для каждого устройства хранится окно последних 10 сообщений
- Если все 10 сообщений удовлетворяют условию `a > 5` → создаётся алерт
- Старые сообщения (>60 сек) автоматически удаляются из окна

**Пример:** 10 подряд сообщений с `a > 5` → алерт `window-a-gt-5-n-10`

### Структура алерта

```json
{
  "id": "...",
  "ruleId": "instant-a-gt-5",
  "deviceId": "device-1",
  "kind": "instant",
  "windowSize": 1,
  "condition": "payload.a > 5",
  "triggeredAt": "2026-01-07T12:00:00Z",
  "payloadSnapshot": {"a": 7},
  "correlationId": "uuid-..."
}
```

---

## 📊 Метрики

### IoT Controller

| Метрика | Тип | Описание |
|---------|-----|----------|
| `iot_messages_received_total` | Counter | Всего получено сообщений |
| `iot_messages_processed_total` | Counter | Успешно обработано |
| `iot_validation_errors_total` | Counter | Ошибки валидации |
| `iot_processing_time_seconds` | Summary | Время обработки |
| `iot_devices_active` | Gauge | Количество активных устройств |
| `iot_device_messages` | Gauge | Сообщений по устройству |

### Rule Engine

| Метрика | Тип | Описание |
|---------|-----|----------|
| `rules_messages_processed_total` | Counter | Обработано сообщений |
| `rules_alerts_triggered_total{type}` | Counter | Алертов по типу (instant/window) |
| `rules_alerts_by_device_total` | Counter | Алертов по устройству |
| `rules_evaluation_time_seconds` | Summary | Время оценки правил |

---

## 👁️ Наблюдаемость

### Prometheus

Сбор метрик с endpoints:
- `iot-controller:8080/actuator/prometheus`
- `rule-engine:8081/actuator/prometheus`

Конфигурация: `monitoring/prometheus.yml`

### Grafana

Автоматически загружаемый дашборд **"IoT Monitoring Dashboard"**:
- Messages Received / Processed
- Instant / Window Alerts
- Messages Rate (per second)
- Alerts Rate (per second)
- Processing Time
- JVM Heap Memory

### ELK Stack

**Filebeat** собирает логи из Docker контейнеров и отправляет в **Elasticsearch**.

**Kibana** используется для:
- Просмотра логов в реальном времени
- Фильтрации по уровню (ERROR, WARN, INFO)
- Фильтрации по контейнеру
- Поиска по correlationId

**Настройка Kibana:**
1. Открыть http://localhost:5601
2. Перейти в Discover
3. Создать Data View: `filebeat-*`
4. Timestamp field: `@timestamp`

---

## 🌐 Веб-интерфейсы

| Сервис | URL | Логин | Описание |
|--------|-----|-------|----------|
| **Grafana** | http://localhost:3000 | admin / admin | Дашборды метрик |
| **Kibana** | http://localhost:5601 | — | Просмотр логов |
| **Prometheus** | http://localhost:9090 | — | Запросы метрик |
| **RabbitMQ** | http://localhost:15672 | guest / guest | Управление очередями |
| **Mongo Express** | http://localhost:8888 | admin / admin | Просмотр MongoDB |

---

## 📁 Структура проекта

```
IoT-mops/
├── common/                    # Общие DTO классы
│   └── src/main/java/.../dto/
│       ├── Alert.java
│       ├── IngestRequest.java
│       ├── IotMessage.java
│       └── QueueEnvelope.java
│
├── iot-controller/            # HTTP вход
│   ├── Dockerfile
│   └── src/main/java/.../
│       ├── api/IngestController.java
│       ├── service/IngestService.java
│       ├── metrics/IotMetrics.java      # Кастомные метрики
│       └── config/
│
├── rule-engine/               # Обработка правил
│   ├── Dockerfile
│   └── src/main/java/.../
│       ├── service/RuleEngineService.java
│       ├── metrics/RuleMetrics.java     # Кастомные метрики
│       └── config/
│
├── data-simulator/            # Генератор трафика
│   └── src/main/java/.../
│       └── DataSimulatorApplication.java
│
├── monitoring/
│   ├── prometheus.yml         # Конфиг Prometheus
│   ├── filebeat/
│   │   └── filebeat.yml       # Конфиг Filebeat
│   └── grafana/
│       └── provisioning/
│           ├── datasources/   # Автоконфиг источников
│           └── dashboards/    # Автозагружаемые дашборды
│
├── docker-compose.yml         # Оркестрация всех сервисов
└── pom.xml                    # Родительский Maven POM
```

---

## 🔧 Конфигурация

### MongoDB

Коллекции:
- `iotMessage` — входящие сообщения
- `alert` — сгенерированные алерты

### RabbitMQ

- **Exchange:** `iot.events` (topic)
- **Queue:** `iot.events.queue`
- **Routing key:** `device.*`

### Параметры Rule Engine

```yaml
app:
  rules:
    window-size: 10           # Размер окна
    max-window-age-sec: 60    # Максимальный возраст сообщения в окне
  rabbit:
    queue: iot.events.queue
```

---

## 🐛 Troubleshooting

### Контейнер не запускается

```bash
# Проверить логи
docker-compose logs <service-name>

# Пересобрать
docker-compose up --build -d
```

### MongoDB connection refused

Убедитесь, что MongoDB запущен и URI корректный:
```bash
docker-compose logs mongodb
```

### Нет данных в Grafana

1. Проверить что симулятор запущен: `docker-compose logs data-simulator`
2. Проверить Prometheus targets: http://localhost:9090/targets
3. Обновить дашборд (F5)

### Нет логов в Kibana

1. Проверить Filebeat: `docker-compose logs filebeat`
2. Создать Data View в Kibana для `filebeat-*`

---

## 📈 Тестирование

### Отправить одно сообщение

```bash
curl -X POST http://localhost:8080/api/ingest \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"test","ts":"2026-01-07T12:00:00Z","payload":{"a":7,"b":1}}'
```

### Проверить метрики

```bash
# IoT Controller
curl http://localhost:8080/actuator/prometheus | grep iot_

# Rule Engine
curl http://localhost:8081/actuator/prometheus | grep rules_
```

### Проверить MongoDB

Открыть http://localhost:8888 (Mongo Express) → база `iot` → коллекции `iotMessage`, `alert`

---

## 📜 Лицензия

MIT License
